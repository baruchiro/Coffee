language: node_js
node_js: 7
branches:
  only:
  - master
script:
  - echo "Deploy!!"
install:
  - npm install -g firebase-tools
after_success:
  - firebase deploy — project sapir-coffe — token $FIREBASE_TOKEN.